{
  "name": "casino-referral-bot-server",
  "version": "1.0.0",
  "description": "Casino Referral Bot Backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build": "cd ../client && npm install && npm run build",
    "build-client": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "express-validator": "^7.2.0",
    "helmet": "^7.1.0",
    "multer": "^2.2.0",
    "pg": "^8.13.0",
    "telegraf": "^4.16.3"
  }
}
